{
  "name": "multi-ai-router",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "bun": ">=1.3.0"
  },
  "scripts": {
    "build": "bin/build",
    "build:types": "bun run --filter '*' build",
    "check": "bin/check",
    "db:generate": "bin/db generate",
    "db:migrate": "bin/db migrate",
    "dev": "bin/dev",
    "fmt": "biome check --write .",
    "lint": "biome check .",
    "setup": "bin/setup",
    "test": "bin/test",
    "typecheck": "tsc --build --verbose"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.5",
    "@types/bun": "^1.3.0",
    "typescript": "^5.9.0"
  }
}
